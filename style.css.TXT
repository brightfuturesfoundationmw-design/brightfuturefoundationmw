/*BFF BRAND COLORS*/
:root{
--bff-teal: #00bfa5;
--bff-dark: #0f1115;
--bff-light:#f8f9fa;
}

*{margin: 0; padding;0; box sizing: border-box}
body {font-family: 'sgoe UI',Arial, sans-serif; color: #333; line-height: 1.7;}
.contianer {max-width:1200px; margin:0 auto; padding:o 40px;}

/*NAVBAR*/
header{
 position:sticky;
 top: 0;
 background: white;
 box-shadow: 0 2px 15px rgba(0,0,0,0.06);
 z-index: 1000;
 padding 15px 0;
}
.nav{display: flex; justfy-content: space-between; align-items: center;}
.logo{display: flex: align-items: center; gap: 12px; font-weight: 700; font-size: 18px; color: var(--bff-dark);}
.logo img {height: 50px;}
nav a{text-decoration: none; color: var(--bff-dark); font-weight: 600; margin: 0 12px; transition: 0.3s;}
nav a:hover {color: var(--bff-teal);}
.btn-donate{
background:var(--bff-teal);
color:white;
padding:12px 24px;
border-radius:50px;
font-weight:700;
text-decoration: none;
border: none;
cursor: pointer;

}
.btn-donate:hover {background: #00a58f; transform: translatey(-2px;}

/* HERO SECTION - 2 colums*/
.hero{
  padding: 100px 0;
  background: var(--bff-light);
}
.hero-grid{
display: grid;
grid-template-columns: 1fr 1fr;
gap: 60px;
align-items: center;
}

.hero-text h1{
 font-size: 48px;
 color: var(--bff-dark);
 line-height: 1.2;
 margin-bottom: 20px
}

.hero-text p{
  font-size: 17px;
  color: #555
  margin-bottom: 30px;
}
.hero-buttons {display: flex: gap: 15px; flex-wrap: wrap;{
.btn-primary{
 background:var(--bff-teal);
 color: white;
 padding:16px 32px
 border-radius:50px
 font-weight:700;
 text-decoration:none;
 display:inline-block
 transition: 0.3s;
}
 btn-primary:hover {transform:translateY(-3px); box-shadow:0 8px 20px rgba(0,191,165,0.3);}
 btn-secondary{
 background:transparent
 border: 2px solid var(--bff-dark);
 padding:14px 30px;
 border-radius:50px;
 font-weight:700;
 text-decoration:none
 display:inline-block
 transition: 0.3s
}
.btn-secondry:hover {background: var(--bff-dark); color: white;}

.hero-image img{
 border-radius:24px;
 box-shadow:0 20px 50px rgba(0,0,0,0.12);
 width: 100%;
}

/*mobile*/
@media (max-width: 900px) {
.hero-grid {grid-template-columns: 1fr;}
.hero-text h1{font-size: 36px;}
}

